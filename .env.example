# Optional: when not in development, override API base (no trailing slash).
# Dev server uses Next rewrites: `/api/*` → external API (see next.config.js).
NEXT_PUBLIC_APP_API_BASE_ENDPOINT=

# If you set `basePath` in next.config.js, set the same value here for public assets (e.g. /partners).
NEXT_PUBLIC_BASE_PATH=

# Canonical site URL for Open Graph metadata (optional).
NEXT_PUBLIC_SITE_URL=https://assetwise.co.th
